version: '3.2'

services:
    pluginComponentUse:
        image: synbiohub/component-use-plugin:snapshot
        ports:
            - "8080:5000"
        volumes:
            - type: volume
              source: pluginComponentUse
              target: /mnt
        dns: 8.8.8.8
        restart: always

volumes:
    pluginComponentUse:
