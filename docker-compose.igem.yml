version: '3.2'

services:
    pluginIgem:
        image: synbiohub/igem-plugin:snapshot
        ports:
            - "3000:3000"
        volumes:
            - type: volume
              source: pluginIgem
              target: /mnt
        dns: 8.8.8.8
        restart: always
        depends_on:
            - synbiohub

volumes:
    pluginIgem:
