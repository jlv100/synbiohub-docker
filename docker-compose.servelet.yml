version: '3.2'

services:
    servelet:
        image: synbiohub/synbiohub/plugin-serve-test:snapshot
        ports:
            - "8081:5000"
        volumes:
            - type: volume
              source: servelet
              target: /mnt
        restart: always
        dns: 8.8.8.8
        depends_on:
            - synbiohub

volumes:
    servelet:
