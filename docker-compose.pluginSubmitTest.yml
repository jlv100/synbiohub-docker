version: '3.2'

services:
    PluginSubmitTest:
        image: synbiohub/plugin-submit-test:snapshot
        ports:
            - "8083:5000"
        volumes:
            - type: volume
              source: PluginSubmitTest
              target: /mnt
        dns: 8.8.8.8
        restart: always
        depends_on:
            - synbiohub

volumes:
    PluginSubmitTest:
