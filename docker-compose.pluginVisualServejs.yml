version: '3.2'

services:
    pluginVisualServejs:
        image: synbiohub/plugin-serve-test:snapshot
        ports:
            - "8086:5000"
        volumes:
            - type: volume
              source: pluginVisualServejs
              target: /mnt
        restart: always
        dns: 8.8.8.8
        depends_on:
            - synbiohub

volumes:
    pluginVisualServejs:
