version: '3.2'

services:
    pluginVisualServelet:
        image: synbiohub/plugin-serve-test:snapshot
        ports:
            - "8086:5000"
        volumes:
            - type: volume
              source: pluginVisualServelet
              target: /mnt
        restart: always
        dns: 8.8.8.8
        depends_on:
            - synbiohub

volumes:
    pluginVisualServelet:
